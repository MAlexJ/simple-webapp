Block

The basic construct for structured templates is the block tag.
Blocks are used for inheritance and act as placeholders and replacements at the same time.

Block tags are useful when defining structured templates, that is, extending templates
which already expose a set of blocks.
Extendable Template

An extendable template is a template which exposes a set of blocks which can be
replaced by templates extending it. Let’s take a look at the example below. 